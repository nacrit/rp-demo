services:
  rp-demo:
    container_name: rp-demo
    image: rp-demo:latest
    volumes:
      - /opt/address-export/rp-demo/log:/opt/address-export/rp-demo/log
    restart: unless-stopped
    ports:
      - "8888:8888"